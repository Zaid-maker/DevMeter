name: Pre-Release Extension

on:
  push:
    branches:
      - prerelease
    paths:
      - 'extension/**'
  workflow_dispatch:
    inputs:
      publish:
        description: 'Publish to marketplaces (yes/no)'
        required: false
        default: 'yes'

jobs:
  prerelease:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.10

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Dependencies
        working-directory: ./extension
        run: bun install

      - name: Bump Pre-Release Version
        working-directory: ./extension
        # Bumps patch and appends a pre-release build number so it never
        # collides with a stable release (e.g. 0.1.6 → 0.1.7-pre.1234567890)
        run: |
          TIMESTAMP=$(date +%s)
          CURRENT=$(node -p "require('./package.json').version")
          # Strip any existing pre-release suffix, then bump patch
          BASE=$(echo "$CURRENT" | sed 's/-pre\.[0-9]*//')
          IFS='.' read -r MAJOR MINOR PATCH <<< "$BASE"
          NEXT_PATCH=$((PATCH + 1))
          PRE_VERSION="${MAJOR}.${MINOR}.${NEXT_PATCH}-pre.${TIMESTAMP}"
          npm version "$PRE_VERSION" --no-git-tag-version
          echo "PRE_VERSION=$PRE_VERSION" >> "$GITHUB_ENV"

      - name: Build Extension
        working-directory: ./extension
        run: bun run build

      - name: Package Extension (.vsix)
        working-directory: ./extension
        run: npx -y @vscode/vsce package --pre-release --out devmeter-prerelease.vsix

      - name: Upload .vsix Artifact
        uses: actions/upload-artifact@v4
        with:
          name: devmeter-prerelease-vsix
          path: extension/devmeter-prerelease.vsix
          retention-days: 14

      - name: Publish to VS Marketplace (pre-release)
        if: ${{ github.event_name == 'push' || github.event.inputs.publish == 'yes' }}
        working-directory: ./extension
        run: npx -y @vscode/vsce publish --pre-release -p ${{ secrets.VSCE_PAT }}

      - name: Publish to Open VSX (pre-release)
        if: ${{ github.event_name == 'push' || github.event.inputs.publish == 'yes' }}
        working-directory: ./extension
        run: npx -y ovsx publish --pre-release -p ${{ secrets.OVSX_PAT }}
